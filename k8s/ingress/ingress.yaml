apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: microservices
spec:
  ingressClassName: ingress
  rules:
    - host: 134.209.115.59
      http:
        paths:
          - path: /api/fale-conosco
              pathType: Prefix
              backend:
                service:
                  name: faleconosco-svc
                  port:
                    number: 8082
